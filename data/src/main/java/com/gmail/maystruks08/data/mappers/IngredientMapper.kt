package com.gmail.maystruks08.data.mappers

import com.gmail.maystruks08.domain.entity.Product
import javax.inject.Inject

class IngredientMapper @Inject constructor(){

//    fun toIngredientsList(mapIngredients: MutableMap<String, Int>): List<Product>{
//        return mapIngredients.map {
//            Product(it.key, it.value)
//        }
//    }
}